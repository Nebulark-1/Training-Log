# Copy this file to .env and fill in the two values from
# https://www.strava.com/settings/api
#
# When you create the API application, set
#   Authorization Callback Domain = 127.0.0.1
# so the one-time `auth` step can catch the redirect locally.

STRAVA_CLIENT_ID=
STRAVA_CLIENT_SECRET=
